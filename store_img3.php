<?php 
   if($_POST){
      echo "hello world";
      // $item = $_POST["Item"];
      // $room = $_POST["Room"];
      // $store = $_POST["Store"];
      // $points = $_POST["Point"];
      // $target_dir = "uploads/";
      // $target_file = $target_dir . basename($_FILES["Image"]["name"]);
      // echo "$item, $room, $store, $points, $target_dir, $target_file";
      
      // print_r($_FILES);

      // $images = $target_file;
      // $image = $_POST["Image"];
      // $data = $_REQUEST['base64data'];
      // $img = explode('base64', $data);
      // file_put_contents('img.png', base64_decode($img[1])); 
      // print_r($img); 
      // $images = $_FILES["Image"];   

      // $data = 'data:image/png;base64,AAAFBfj42Pj4'; // THERE AR SOME RESULTS
      // list($type, $data) = explode(';', $data); 
      // list(, $data)      = explode(',', $data);
      // $data = base64_decode($data);
      // print_r($image);
      // file_put_contents('uploads/image.png', $data);
      // echo $target_file;
      // print_r($_FILES);

   //    if (move_uploaded_file($_FILES["Image"]["tmp_name"], $target_file)) {
   //       echo "The file ". basename( $_FILES["Image"]["name"]). " has been uploaded.";
   //   } else {
   //       echo "Sorry, there was an error uploading your file.";
   //   }
    
   }
?>